{
  "name": "lifechess-level-1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_OPTIONS=--experimental-sqlite tsx watch src/server.ts",
    "start": "node --experimental-sqlite dist/server.js",
    "build": "tsc",
    "test": "NODE_OPTIONS=--experimental-sqlite vitest run",
    "tournament": "NODE_OPTIONS=--experimental-sqlite tsx src/tournament.ts"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
